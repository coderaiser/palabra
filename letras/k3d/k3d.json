{
    "name": "k3d",
    "url": "https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh",
    "bin": "{{ name }}",
    "env": {
        "K3D_INSTALL_DIR": "{{ directorio }}/{{ name }}",
        "USE_SUDO": "false"
    },
    "executable": true,
    "test": [
        "{{ name }} -v"
    ],
    "beforeInstall": [
        "mkdir -p {{ directorio }}/{{ name }}"
    ]
}
