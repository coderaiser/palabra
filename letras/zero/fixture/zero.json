[
    "mkdir -p ~/.local/bin",
    "ZERO_INSTALL_DIR=~/.local/share/zero ZERO_VERSION=latest bash -c \"$(curl -fsSL https://zerolang.ai/install.sh)\"",
    "ln -fs ~/.local/share/zero/zero ~/.local/bin/zero",
    "ZERO_INSTALL_DIR=~/.local/share/zero ZERO_VERSION=latest zero --version"
]