{
    "name": "indra",
    "version": "1.0.1",
    "bin": "{{ name }}",
    "executable": true,
    "preparado": true,
    "test": [
        "{{ name }} --version"
    ],
    "alias": {
        "arch": {
            "x64": "amd64"
        }
    },
    "url": "https://github.com/coderaiser/{{ name }}/releases/download/v{{ version }}/{{ name }}_{{ version }}_{{ platform }}_{{ arch }}",
    "rename": "{{ name }}-{{ platform }}-{{ arch }}"
}
